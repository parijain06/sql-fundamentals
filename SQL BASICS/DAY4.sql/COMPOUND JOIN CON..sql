-- COMPUND JOIN CONDITIONS

-- used to join two tables using MULTIPLE COLUMNS
-- it is used when one column is not enough to correctly match rows between tables
-- or used when one column is not enough to uniquely identify matching record between two tables or each record

-- SELECT *
-- FROM order_items oi
-- JOIN order_item_notes oin
--    ON oi.order_id = oin.order_id
--    AND oi.product_id = oin.product_id




